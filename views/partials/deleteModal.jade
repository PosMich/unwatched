.modal-header(resize-modal)
    h3.modal-title
        | Delete "{{item.name}}"
.modal-body
    | Are you sure you want to delete this shared item:
    table
        tr
            td
                b Name:
            td {{item.name}}
        tr(ng-if="item.size!=0 && item.size!=''")
            td(ng-if="item.category=='code'")
                b Lines:
            td(ng-if="item.category=='code'")
                | {{item.size}}
            td(ng-if="item.category!='code'")
                b Size:
            td(ng-if="item.category!='code'")
                | {{item.size | humanReadableFileSize}}
        tr
            td
                b Author:
            td {{users[item.author].name}}

        tr(ng-if="item.contributors")
            td
                b Contributors:
            td
                span(ng-repeat="contributor in item.contributors")
                    | {{users[contributor.id].name}}
        tr
            td
                b Created:
            td {{item.created | date: 'dd.MM.yyyy - HH:mm'}}

        tr(ng-if="item.uploaded")
            td
                b Uploaded:
            td {{item.uploaded | date: 'dd.MM.yyyy - HH:mm'}}

        tr(ng-if="item.last_edited")
            td
                b Last edited:
            td {{item.last_edited | date: 'dd.MM.yyyy - HH:mm'}}

        tr(ng-if="item.mime_type")
            td
                b MIME-Type:
            td {{item.mime_type}}

        tr(ng-if="item.extension!=''")
            td
                b Extension:
            td {{item.extension}}

        tr
            td
                b Category:
            td {{item.category}}

.modal-footer
    button.btn.btn-danger(ng-click="ok()") Delete
    button.btn.btn-default(ng-click="cancel()") Cancel
